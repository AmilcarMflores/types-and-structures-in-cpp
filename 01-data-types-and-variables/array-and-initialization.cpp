#include <iostream>
using namespace std;

int main() {
    // Declare and initialize an array of integers
    int numbers[5] = {1, 2, 3, 4, 5};

    // Print the array elements
    for (int i = 0; i < 5; i++) {
        cout << numbers[i] << " ";
    }
    cout << endl;
    return 0;
}